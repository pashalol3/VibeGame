# Лог изменений — МАСКАРАД

## 2026-06-20

### Добавлены правила работы агента

- Весь код проверять логически.
- Самостоятельно коммиты не делать.
- При сомнениях спрашивать пользователя.

### Созданы файлы

- `package.json` — зависимости: Express, Socket.IO; dev: nodemon.
- `server.js` — основной игровой сервер.
- `public/index.html` — клиентский интерфейс.
- `public/styles.css` — стили интерфейса.
- `public/game.js` — клиентская логика.
- `AGENTS.md` — правила игры и технический контекст.
- `LOG.md` — этот файл.

### Логическая проверка и исправления

1. **Роли и способности после обмена масками.** Все проверки способностей (Вор, Дворецкий, Детектив) теперь используют `currentRole` вместо исходной `role`. Это значит, что после обмена масками новый владелец роли получает её способности.
2. **Перезапуск таймеров воров.** В начале каждой ночной фазы теперь вызывается `resetThiefTimers`, который сбрасывает прогресс кражи и личные цели каждого вора.
3. **Личные цели воров.** Убрана общая переменная `room.thiefTarget`; теперь каждый вор хранит `p.thiefTarget`. Это корректно работает при 1–2 ворах и после обменов ролями.
4. **Персональная слежка.** Слежка теперь учитывается отдельно для каждого детектива (`surveillanceUsedThisRound`) — каждый детектив может объявить слежку один раз за раунд.
5. **Пенальти за ошибочное обвинение.** Реализовано правильное отслеживание: игрок теряет голос в следующем раунде, а не в текущем, и не сбрасывается сразу. Добавлено поле `penaltyRound`.
6. **Раскрытие ролей при окончании игры.** В конце игры теперь раскрываются текущие роли всех игроков, а не только исходные роли воров.
7. **Прогресс кражи.** `stealProgress` и `stolenThisRound` теперь отправляются только самому игроку (`myStealProgress`, `myStolenThisRound`), чтобы не раскрывать вора другим.
8. **Двойная фаза Итог.** Исправлена ошибка, при которой неправильное обвинение запускало вторую фазу Итог поверх первой.
9. **Закрытие обвинений по таймауту.** Если время обвинения заканчивается без большинства, обвинение автоматически закрывается.
10. **Валидация скорости движения.** На сервере добавлена проверка максимального перемещения за отведённое время.
11. **Защита от повторного старта.** Добавлена проверка `room.phase !== 'lobby'` в обработчик `startGame`.
12. **Клиентский клик по комнате.** Исправлено: при клике по комнате в ночной фазе Вор больше не одновременно выбирает игрока под курсором.
13. **Удалён неиспользуемый код.** Убраны функции `broadcast`, `checkGameOverAfterArrest` и лишний `publicState` emit.

### Зависимости

- Установлены зависимости через `npm install` (Express, Socket.IO, nodemon).
- Сервер запускается без синтаксических ошибок.

### Предстоящие изменения / вопросы к пользователю

- Уточнить несколько логических моментов правил (см. ответ агента).
- Добавить обработку отключений игроков (реконнект или завершение игры).
- Минимальное количество игроков снижено до 2 для удобства тестирования (в `server.js` и `public/game.js`).
- Исправлено движение: теперь локальная позиция является авторитетной, сервер лишь транслирует её; убран эффект отбрасывания назад; добавлена интерполяция позиций и throttle на отправку движения (50 мс), серверный throttle на broadcast состояния.
- Увеличена частота серверного broadcast состояния с 50 до 30 мс для более плавного отображения других игроков.
- Уменьшен коэффициент интерполяции позиций других игроков (0.12 вместо 0.25) для плавного движения.
- Правила игры в `AGENTS.md` переписаны более понятно и структурированно: добавлены таблицы ролей, управления, особых ситуаций.
- Исправлены бинды: теперь обрабатываются как стрелочные, так и буквенные клавиши независимо от раскладки (WASD + ЦЫФВ).
- Исправлен выбор цели по клику: добавлена функция `pickTargetAt`, которая выбирает ближайшего игрока под курсором; радиус захвата увеличен до 60 пикселей.
- Добавлен альтернативный способ выбора игрока — клик по имени в списке игроков в правой панели.
- `selectedTargetId` сбрасывается при смене фазы, чтобы ID комнаты не использовался как ID игрока.
- Кнопки действий теперь проверяют, что `selectedTargetId` действительно является ID игрока; панель действий сбрасывается перед рендером кнопок.
- Исправлен масштаб канваса: убран `object-fit: contain`, заданы фиксированные размеры 1000×700.
- Визуальное выделение своего персонажа белой обводкой.
- Уменьшена и упрощена правая панель (220px, компактные шрифты).
- Добавлен системный лог в правой панели с кнопкой «Копировать лог», чтобы пользователь мог присылать его для диагностики.
- Добавлено логирование событий: подключение, получение состояния, уведомления, ошибки, клики по канвасу, выбор целей, клик по обмену масками, появление/скрытие модального окна обмена.
- Временно увеличена дистанция для обмена масками до 200 пикселей для тестирования.
- Добавлена защита `if (!state || !myId) return` в `updateLobby` и диагностический `console.log` для отладки проблемы с кнопкой «Начать игру».
- Исправлена передача `hostId` в клиентское состояние; теперь у создателя комнаты появляется кнопка «Начать игру».
- Исправлена проблема с несколькими ворами: каждый вор теперь хранит свою цель (`p.thiefTarget`), таймеры кражи сбрасываются каждую ночь.
- Исправлена слежка: теперь отслеживается отдельно для каждого детектива (`surveillanceUsedThisRound`).
- Исправлено пенальти за ошибочное обвинение: обвинитель теряет голос в следующем раунде, отслеживается через `penaltyRound`.
- При окончании игры раскрываются текущие роли всех игроков.
- Прогресс кражи (`stealProgress`, `stolenThisRound`) отправляется только самому вору (`myStealProgress`, `myStolenThisRound`).
- Исправлена двойная фаза Итог при неправильном обвинении.
- Добавлено закрытие обвинений по таймауту.
- Убраны неиспользуемые функции `broadcast`, `checkGameOverAfterArrest` и лишний `publicState` emit.
- Обновлена инструкция по запуску в `AGENTS.md`.
- Добавлены правила работы агента в `AGENTS.md`: логическая проверка кода, отсутствие самостоятельных коммитов, вопросы пользователю при сомнениях.
- Исправлен выбор цели: теперь выбор игрока работает по отрендеренным (сглаженным) координатам, клик логируется.
- Временно увеличена дистанция для обмена масками до 200 пикселей для тестирования.
- Добавлено логирование кликов по канвасу и выбора целей.
- Исправлена тряска игрока: теперь позиции сглаживаются на отрисовке.
- Уменьшена и упрощена правая панель (220px, компактные шрифты).
- Добавлен системный лог в правой панели с кнопкой «Копировать лог», чтобы пользователь мог присылать его для диагностики.
- Логирование событий: подключение, получение состояния, уведомления, ошибки, клик по обмену масками.
- Добавлено логирование появления/скрытия модального окна обмена масками на клиенте.
- Исправлены бинды: теперь обрабатываются как стрелочные, так и буквенные клавиши независимо от раскладки (WASD + ЦЫФВ).
- Исправлен выбор цели по клику: добавлена функция `pickTargetAt`, которая выбирает ближайшего игрока под курсором.
- Визуальное выделение своего персонажа белой обводкой.
- Добавлено логирование в `offerMask` для отладки обмена масками.
- Добавлен сброс панели действий перед рендером кнопок.
- Обновлена инструкция по запуску в `AGENTS.md`.
